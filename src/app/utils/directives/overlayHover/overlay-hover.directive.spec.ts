import { OverlayHoverDirective } from './overlay-hover.directive';

describe('OverlayHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayHoverDirective();
    expect(directive).toBeTruthy();
  });
});
